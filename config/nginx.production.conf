# Copyright (C) 2026 Maxim [maxirmx] Samsonov (www.sw.consulting)
# All rights reserved.
# This file is a part of the Sarafan application

server_tokens off;

server {
    listen 80;
    listen [::]:80;
    server_name sb.sw.consulting;
    return 301 https://sb.sw.consulting$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name sb.sw.consulting;
    ssl_certificate /etc/nginx/certificate/s.crt;
    ssl_certificate_key /etc/nginx/certificate/s.key;
    resolver 127.0.0.11 valid=10s ipv6=off;
    set $backoffice_upstream backoffice:8080;
    location / {
        proxy_pass http://$backoffice_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 30s;
    }
}

server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    location = /health {
        access_log off;
        default_type text/plain;
        return 200 "ok\n";
    }

    location / {
        return 404;
    }
}

server {
    listen 80;
    listen [::]:80;
    server_name sarafan.sw.consulting;

    return 301 https://sarafan.sw.consulting$request_uri;
}

server {
    listen 443 ssl default_server;
    listen [::]:443 ssl default_server;
    server_name _;

    ssl_certificate /etc/nginx/certificate/s.crt;
    ssl_certificate_key /etc/nginx/certificate/s.key;

    location / {
        return 404;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name sarafan.sw.consulting;

    ssl_certificate /etc/nginx/certificate/s.crt;
    ssl_certificate_key /etc/nginx/certificate/s.key;

    location / {
        proxy_pass http://ui:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 30s;
    }
}
